name: alpha_password_generator
description: A minimal and stylish password generator app with history and save features.
publish_to: 'none' # پروژه خصوصی
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.0.15
  encrypt: ^5.0.1
  base64: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
